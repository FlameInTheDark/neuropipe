# Append to Word Document

## Purpose
Appends one paragraph per text line to an existing .docx document, keeping
all existing content, styles, and section properties intact.

## Example
`Read Word Text` → `LLM Prompt` conclusion → `Append to Word Document
(C:\Work\report.docx)`.
