# List Excel Worksheets

## Purpose
Lists the worksheets of an Excel workbook in order, as a list of names plus a
count. Use it to discover sheet names before targeting one, or to iterate a
workbook's sheets dynamically.

## Example
`Read Excel Rows` needs the sheet name → `List Excel Worksheets` → the first
entry drives the next node through a variable.
