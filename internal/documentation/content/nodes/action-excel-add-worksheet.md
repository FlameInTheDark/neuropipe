# Add Excel Worksheet

## Purpose
Adds a new worksheet to an Excel workbook, or reports the existing sheet with
the same name when the fail-if-exists option is off.

## Example
`Cron Trigger` monthly → `Add Excel Worksheet (February)` →
`Append Excel Rows` writes the month's log into the fresh sheet.
