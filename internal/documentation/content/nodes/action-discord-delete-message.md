# Delete Discord Message

Delete one message the bot can moderate. Deleting requires the Manage
Messages permission for other authors' messages; missing permissions surface
through the Rejected port with Discord's own reason.
