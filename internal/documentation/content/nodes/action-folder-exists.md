# Folder Exists

## Purpose

Check whether a path exists on the filesystem and is a directory. Returns a boolean result and a summary record.

## Outputs

- `result` — `true` if the path is a directory.
- `summary.exists` — same boolean, in record form.

## Capabilities

Requires **file-read** capability.