# Length

## Purpose

Counts elements of a list, characters of text, or keys of an object.

## Example

`HTTP Request json → Length → Greater Than`.

## Notes

Text length is measured in characters, not bytes. Any other value kind fails the
requesting execution path with a clear error.
