# Create Word Document

## Purpose
Creates a new .docx document from an optional bold title and a body where
every text line becomes one paragraph.

## Example
`LLM Prompt` writing a weekly summary → `Create Word Document
(C:\Work\summary.docx, title "Weekly report")`.
