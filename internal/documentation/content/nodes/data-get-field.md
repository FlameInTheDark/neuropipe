# Get Field

## Purpose
Exposes named, typed outputs from object or list paths.

The default **Value** output is an Any data pin. Add more outputs in the node inspector when one source object needs several fields.

## Example
`Run Terminal Command.Result → Get Field (terminal.output) → LLM Prompt`.
