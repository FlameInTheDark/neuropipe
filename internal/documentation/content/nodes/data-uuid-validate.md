# Validate UUID

## Purpose

Return whether a string is a valid UUID. The input is also forwarded as a **Value** output pin so you can chain this node before a Switch or Branch without losing the original text.

## Outputs

- `value` — the input unchanged.
- `result` — boolean indicating whether the input parsed as a UUID.

## Capabilities

None.